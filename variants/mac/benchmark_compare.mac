/control/macroPath mac

# Run the core benchmark twice so foil-only and backed data land in the same CSV
# and share the exact same RNG stream for percent-level comparisons.
# Foil-only benchmark pass (backing disabled)
/control/alias backingThickness_um 0
/control/execute benchmark_core.mac

# Backed benchmark pass (default 50 µm tungsten backing)
/control/alias backingThickness_um 50
/control/execute benchmark_core.mac
